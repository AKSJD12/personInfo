{
  "buildCommand": "cd personal-site && npm install && npm run build",
  "outputDirectory": "personal-site/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1" }
  ]
}
